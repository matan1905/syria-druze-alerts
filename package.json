{
  "name": "suwayda-alert",
  "version": "1.0.0",
  "description": "Iran missile early warning system for Druze community in Suwayda",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch run server.ts"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
